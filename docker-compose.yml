version: "3.3"
services:
  app:
    build: .
    image: bwilfong122/video_app:latest
    environment:
      - DB_PASSWORD=${DB_PASSWORD}  # Pull the DB_PASSWORD variable from the .env file
    ports:
      - "80:80"
